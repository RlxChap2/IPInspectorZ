{
    "name": "ip-inspector-z",
    "version": "1.0.0",
    "description": "Lightweight app for network and device diagnostics",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "npx tsc -b && vite build",
        "build:app": "npm run build && npm run tauri build",
        "preview": "vite preview",
        "tauri": "tauri",
        "icon": "tauri icon"
    },
    "devDependencies": {
        "@tauri-apps/cli": "^2.0.0",
        "typescript": "^5.3.0",
        "vite": "^5.0.0"
    }
}
